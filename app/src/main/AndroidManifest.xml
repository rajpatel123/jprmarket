<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.jpr.market">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.SkipActivity" />
        <activity android:name=".Activities.LocationActivity" />
        <activity
            android:name=".Activities.NearbyLocationActivity"
            android:theme="@style/AppTheme4" />
        <activity
            android:name=".Activities.SearchActivity"
            android:theme="@style/AppTheme5" />


        <activity android:name=".Activities.CategoryActivity" />
        <activity android:name=".Activities.app_bar_main" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.MyCardActivity" />
        <activity android:name=".Activities.SignupActivity" />
    </application>

</manifest>